# SSH Client Configuration
# Auto-generated by chezmoi from {{ .chezmoi.sourceFile }}
# DO NOT EDIT DIRECTLY. Your changes will be overwritten.

# Global Defaults
Host *
    # Automatically add keys to active agent after first use
    AddKeysToAgent yes
    # Keep connections alive in background
    ServerAliveInterval 60
    ServerAliveCountMax 10
    # Enable connection multiplexing (faster subsequent connections)
    ControlMaster auto
    ControlPath ~/.ssh/sockets/%r@%h-%p
    ControlPersist 600

# GitHub
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519

# GitLab
Host gitlab.com
    HostName gitlab.com
    User git
    IdentityFile ~/.ssh/id_ed25519

# Add any custom hosts below or use chezmoi templates to customize per machine:
# {{ "{{" }}- if eq .chezmoi.hostname "your-work-host" {{ "}}" }}
# Host work-host
#     HostName 10.0.0.1
#     User idan
# {{ "{{" }}- end {{ "}}" }}
