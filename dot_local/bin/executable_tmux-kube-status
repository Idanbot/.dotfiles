#!/usr/bin/env bash

set -euo pipefail

command -v cloud-context >/dev/null 2>&1 || exit 0
context="$(cloud-context prompt kubectl 2>/dev/null)" || exit 0
[[ -n "$context" ]] || exit 0
printf '\U000f10fe %s' "$context"
