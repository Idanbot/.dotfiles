# Ignore README and CI files — they're repo metadata, not dotfiles
README.md
LICENSE
packages.yaml
scripts/
tests/
.github/
.gitignore

# Platform-specific ignores
{{ if .is_wsl }}
# Skip desktop/GUI configs on WSL
dot_config/private_kitty
{{ end }}
